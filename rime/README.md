# Rime双拼（自然码）安装指南

- 安装 Rime
- 先删除 `用户文件夹` 内的全部文件，再将`config.zip`压缩包内的所有文件复制进去

### 用户文件夹位置

#### macOS（Squirrel）

```text
~/Library/Rime
```

#### Windows（小狼毫 Weasel）

```text
%APPDATA%\Rime
```

通常实际路径类似：

```text
C:\Users\你的用户名\AppData\Roaming\Rime
```

---

- 重新部署（Deploy）
- `F4` 切换双拼输入法
